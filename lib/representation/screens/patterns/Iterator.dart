import 'package:flutter/material.dart';

class Iterator extends StatelessWidget {
  static const String SCREEN_ROUTE = "./Iterator";
  const Iterator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Iterator'),
      ),
    );
  }
}
