import 'package:flutter/material.dart';

class Proxy extends StatelessWidget {
  static const String SCREEN_ROUTE = "./Proxy";
  const Proxy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Proxy'),
      ),
    );
  }
}
