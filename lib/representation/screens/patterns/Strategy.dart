import 'package:flutter/material.dart';

class Strategy extends StatelessWidget {
  static const String SCREEN_ROUTE = "./Strategy";
  const Strategy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Strategy'),
      ),
    );
  }
}
